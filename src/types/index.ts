export interface User {
    id: number
    email: string
    name: string
}

export interface Event {
    // id: number
    // title: string
    // description: string
    // dateTime: string
    // location: string
    // organizer: User
    // participants: User[]
    // status: 'draft' | 'published' | 'cancelled'
}