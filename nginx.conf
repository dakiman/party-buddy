server {
    listen 8094;
    listen [::]:8094;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Long cache for hashed Vite assets
    location /assets/ {
        access_log off;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Proxy API calls to the BE running on the host (network_mode: host)
    location /api/ {
        proxy_pass         http://127.0.0.1:8093/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # SPA routing — every other path falls back to index.html.
    # The `no-store` header keeps the SPA shell uncached so deploys are picked up
    # immediately even if a CDN/proxy is later placed in front (the hashed assets
    # under /assets/ remain immutable; only index.html is no-store).
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    }

    # Don't log favicon/robots noise
    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }
}
